# Optional — VayuShield works fully out of the box without these.
# Add a Gemini API key to unlock AI-personalised health tips in the
# Air Quality Intelligence pillar.
GEMINI_API_KEY=

# Optional — if you have real CPCB/IMD API access, wire it into
# app/api/air-quality/route.ts and app/api/cyclone/route.ts.
# CPCB_API_KEY=
# IMD_API_KEY=
